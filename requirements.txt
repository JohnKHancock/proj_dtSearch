# NLQ to dtSearch - Direct dependencies
# Install with: pip install -r requirements.txt

# Core application (5.27.0+ fixes gradio_client bool schema TypeError on HF Spaces)
gradio>=5.27.0
gradio_client>=1.4.0
# Pin huggingface_hub<1.0 for Gradio compatibility (HfFolder was removed in v1.0)
huggingface_hub>=0.23.0,<1.0
openai>=2.15.0
python-dotenv>=1.0.0

# Optional: Jupyter notebook support (for setup.ipynb, __testing.ipynb)
ipykernel>=7.0.0

# Testing
pytest>=7.0.0

# Scraper module (scraper.py)
beautifulsoup4>=4.12.0
requests>=2.28.0
